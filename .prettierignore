# Generated by `supabase gen types typescript` — never hand-edited, and
# reformatting it here would be undone by the next generation run. The matching
# exclusion for the linter lives in eslint.config.js; both are needed, since
# `npm run format` reaches this file even though lint-staged does not.
src/db/database.types.ts

# Build output
dist/
.astro/

# Supabase CLI scratch (link state) — machine-written, gitignored
supabase/.temp/

# Project docs (PRD, roadmap, plans, change records) — prose authored by humans
# and agents, not code. Prettier's prose reflow and table re-alignment produce
# large, meaningless diffs and once mangled a glob (`*` → `_`) in these files.
# Excluded from both `npm run format` and the lint-staged pre-commit hook.
context/

# Toolkit-managed (`@przeprogramowani/10x-cli`) — skills, prompts and the manifest
# are machine-written and re-applied on every lesson bump. The manifest stores a
# contentHash per file, so a Prettier pass here makes the toolkit see its own
# files as hand-edited and rewrite them back on the next run — a loop that
# produced pure formatting churn in 52d8c0d. Same reasoning as `context/` above.
.claude/
